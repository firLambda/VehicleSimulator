anacondaだとうまく動くことがある

動作確認済の環境
・openpyxlのみインストール時のバージョンを指定したほうがいい
インストールの方法は
"pip install openpyxl==3.0.0"

・"pip list"でバージョンの確認が可能

・"pip uninstall 〇〇"で○○パッケージのアンインストールが可能。

numpyがエラーをすることがあるがだいたいopenpyxlが悪い
可視化に失敗する時はopenpyxlのバージョンを疑うべし

